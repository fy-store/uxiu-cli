import type { Options } from '../../build/types.js'

/**
 * 定义构建选项
 * - 基于 tsdown 构建, 支持 tsdown 所有配置选项
 * @param options 配置选项
 * @returns
 */
export function defineBuild(options: Options): Options {
	return options
}
